# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel

# Env local — nunca versionar
.env
.env*
!.env.example

# Build do Next — nunca versionar
.next/

# artefato de ferramenta local, não é código
.qlaude/
tsconfig.tsbuildinfo

# lockfile gerado por `deno test`/`deno check` local (edge functions) — nunca foi versionado
deno.lock
supabase/.temp/

# Graphify: versionar apenas relatório, HTML e JSON canônicos
graphify-out/.graphify_*
graphify-out/cache/
graphify-out/cost.json
graphify-out/manifest.json
graphify-out/[0-9][0-9][0-9][0-9]-*/
